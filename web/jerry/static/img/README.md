# Static images

`seinfeld-cast.jpg` — full-viewport background. The Layout template uses
`background-size: cover; background-position: center` and a tint overlay so
text stays readable in either theme. Replace the file in place to swap the
backdrop; no code change needed.
